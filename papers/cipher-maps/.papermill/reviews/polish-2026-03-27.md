# Pre-Submission Polish Check: Cipher Maps

**Paper**: `paper/cipher_maps.tex`
**Date**: 2026-03-27
**Reviewer**: Automated (Claude Code)
**Build**: `make` (pdflatex x3 + bibtex)

---

## 1. Build Verification

- [PASS] Paper compiles to PDF (21 pages, 356056 bytes) with zero LaTeX errors
- [PASS] Zero undefined references (no "??" in output)
- [PASS] Zero undefined citations
- [PASS] Zero overfull/underfull hbox warnings
- [PASS] Zero multiply-defined labels
- [FAIL] 6 hyperref warnings: math tokens in PDF section bookmarks (lines 312, 343) -- Section headings "Representation Uniformity ($\delta$-bounded)" and "Correctness ($\eta$-bounded)" contain math that hyperref cannot render as Unicode in PDF bookmarks. Fix with `\texorpdfstring{$\delta$}{delta}` etc.
- [FAIL] 3 pdfTeX duplicate destination warnings: `table.1`, `table.2`, `table.3` -- All three `\begin{table}[ht]` floats are placed by TeX on a different page than their initial position, causing hyperref to create duplicate PDF anchors. This can cause hyperlinks to tables to jump to the wrong page. Fix by adding `\usepackage[hypcap=true]{caption}` or placing `\phantomsection` before `\caption`, or changing float specifiers.
- [PASS] Page count: 21 pages (including references on pp. 20-21)

## 2. Metadata

- [PASS] Title: "Cipher Maps: Total Functions as Trapdoor Approximations" -- appropriate and descriptive
- [PASS] Author: Alexander Towell, `lex@metafunctor.com`
- [PASS] Date: "March 2026" (specific month/year, not `\today`)
- [PASS] Abstract present (14 lines), concise, accurately summarizes contributions
- [FAIL] No keywords provided -- Not an issue for all venues, but many require them (e.g., ACM, IEEE). If targeting a specific venue, add `\paragraph{Keywords.}` or the venue's keyword macro.
- [PASS] No affiliation listed beyond email -- Acceptable for independent/preprint submission

## 3. Citations and Bibliography

- [PASS] Every `\cite{}` key has a matching bib entry (18 citations, 18 bib entries)
- [PASS] Every bib entry is cited at least once (zero uncited entries)
- [PASS] No duplicate bib entries
- [PASS] Bib entries have consistent formatting (all have year, title, venue/journal)
- [PASS] Author names formatted correctly throughout (Last, First or full names)
- [PASS] No obviously wrong years or venues
- [PASS] Self-citation clearly identifiable: `bernoulli-types` (Towell, 2024) is the only self-citation
- [FAIL] Self-citation `bernoulli-types` is `@unpublished` with note "Working paper" -- If this paper has been posted to arXiv or has a more specific status by now, the bib entry should be updated with a URL, arXiv ID, or institution. "Working paper" is vague for a published reference list.

## 4. Cross-References

- [PASS] Every `\ref{}` resolves -- zero undefined references in the log
- [PASS] No dangling references (every referenced label exists)
- [FAIL] 27 orphan labels (defined but never referenced by `\ref` or `\cref`):
  - Section labels: `sec:intro`, `sec:related`, `sec:abstraction`, `sec:uniformity`
  - Subsection labels: `subsec:layers`, `subsec:acceptance`, `subsec:tractability`, `subsec:encrypted-search`
  - Definitions: `def:totality`, `def:correctness`, `def:trusted`, `def:untrusted`
  - Theorems/Props: `thm:composition-correctness`, `thm:space-optimal`, `prop:entropy-space`, `prop:membership-fp`, `prop:bucketed`, `prop:deniability`, `prop:granularity`, `cor:chain`
  - Tables: `tab:params`, `tab:guarantees`, `tab:comparison`
  - Equations: `eq:composition`, `eq:space`
  - Remarks: `rem:boolean-asymmetry`
  - Algorithm: `alg:singular-hash`

  These are not errors, but orphan labels suggest missed opportunities to cross-reference. Notable: `thm:composition-correctness` (the early statement of the composition theorem in Section 4) is never referenced; only the restated `thm:composition` (Section 8) is referenced. Tables 1-3 are never referenced by number in the text. The algorithm is never referenced by number.
- [PASS] Section numbering is sequential (1-10 + unnumbered Acknowledgments)
- [PASS] Forward references are clear (e.g., "See Section 8 (Theorem 8.1)" pattern)

## 5. Figures and Tables

- [PASS] No figure environments in the paper (no figures to check)
- [PASS] All 3 float tables have captions: "Cipher map parameters" (tab:params), "What the four properties guarantee against $U$" (tab:guarantees), "Comparison of the three constructions" (tab:comparison)
- [PASS] Algorithm 1 has a caption: "Singular Hash Map Construction"
- [FAIL] No float table or algorithm is referenced by number in the text -- Tables 1-3 and Algorithm 1 are never cross-referenced with `\ref{tab:params}` etc. The reader encounters them only by proximity. Standard practice is to reference them explicitly (e.g., "Table 1 summarizes the parameters").
- [PASS] Captions are informative (not just "Table 1")
- [PASS] All tables use booktabs formatting (`\toprule`, `\midrule`, `\bottomrule`)
- [PASS] 7 inline tabular environments (non-float) used for parameter instantiation summaries -- acceptable for this type of inline data

## 6. Notation Consistency

- [PASS] Core notation used consistently: $\eta$ (correctness), $\varepsilon$ (noise decode), $\mu$ (value cost), $\delta$ (uniformity), $K(x)$ (multiplicity), $p$ (entanglement)
- [PASS] $\fhat$, $\ghat$, $\enc$, $\dec$ used consistently throughout
- [PASS] Notation introduced before first use (Section 3 defines the tuple, Section 4 defines parameters)
- [PASS] $h$ consistently denotes the cryptographic hash function
- [PASS] No dead notation (all defined symbols are used)
- [PASS] No undefined symbols -- all symbols are introduced before use

## 7. Prose Quality

- [PASS] No typos detected in full scan
- [PASS] No sentence fragments
- [PASS] No TODO/FIXME/XXX/TBD/placeholder text
- [FAIL] 11 source-code comments with change-tracking annotations (`% SIMPLIFIED:`, `% FIXED:`, `% IMPROVED:`) remain in the .tex source -- These are invisible in the PDF output but should be cleaned before submission to avoid revealing revision history to reviewers/editors who inspect source. Lines: 1, 91, 209, 425, 592, 820, 883, 903, 1230, 1393, 1508.
- [PASS] Section transitions are clear and logical
- [PASS] No orphaned TODO/FIXME/XXX in visible text

## 8. Formatting

- [PASS] Theorem environments used consistently: `definition`, `theorem`, `lemma`, `corollary`, `proposition`, `remark`, `example` -- all properly defined with `amsthm`
- [PASS] All 12 proofs properly open and close (`\begin{proof}` / `\end{proof}` balanced)
- [PASS] Proofs end with QED symbol (automatic via `amsthm`)
- [FAIL] 14 of 16 numbered equations are never cross-referenced -- Only `eq:composition` (line 389) and `eq:space` (line 434) have labels, and even those labels are never referenced. Convention is that numbered equations should be referenced; unreferenced display equations should use `equation*` or `\[...\]` to suppress numbering.
- [PASS] No widows/orphans detected (zero underfull vbox warnings)
- [PASS] Consistent use of math mode throughout

## 9. Venue Readiness

- [PASS] Standard `article` class with `11pt`, `1in` margins -- suitable for preprint/arXiv
- [FAIL] No specific venue template -- The paper uses a generic `article` class. If targeting a specific venue (the CLAUDE.md mentions USENIX for a sibling paper), the appropriate style file would be needed. For arXiv preprint, this is fine.
- [PASS] Page count (21pp) is reasonable for a theory paper at most CS venues
- [FAIL] No MSC/ACM classification codes -- Many venues require these

---

## Summary

| Category | PASS | FAIL |
|----------|------|------|
| 1. Build Verification | 6 | 2 |
| 2. Metadata | 4 | 1 |
| 3. Citations/Bibliography | 7 | 1 |
| 4. Cross-References | 4 | 1 |
| 5. Figures/Tables | 5 | 1 |
| 6. Notation Consistency | 6 | 0 |
| 7. Prose Quality | 5 | 1 |
| 8. Formatting | 5 | 1 |
| 9. Venue Readiness | 2 | 2 |
| **Total** | **44** | **10** |

## Priority Fixes (by impact)

1. **[HIGH] Numbered equations without cross-references** (14 equations) -- Replace `\begin{equation}...\end{equation}` with `\[...\]` for unreferenced display math. This is the most visible formatting issue a reviewer would notice.

2. **[HIGH] Float tables/algorithm never referenced by number** -- Add explicit references like "Table~\ref{tab:params} summarizes..." in the surrounding text. Readers and reviewers expect floats to be referenced.

3. **[MEDIUM] Hyperref duplicate table destinations** -- The 3 duplicate destination warnings mean hyperlinks to tables may jump to the wrong page. Fix with `\usepackage[hypcap=true]{caption}` or adjust float placement.

4. **[MEDIUM] Hyperref bookmark warnings for math in section titles** -- Use `\texorpdfstring` for the two section headings containing `$\delta$` and `$\eta$`.

5. **[LOW] Clean source comments** -- Remove the 11 `% SIMPLIFIED/FIXED/IMPROVED` tracking comments before submission.

6. **[LOW] Update `bernoulli-types` bib entry** -- Provide a URL, arXiv ID, or more specific status than "Working paper".

7. **[LOW] 27 orphan labels** -- Not errors, but consider either referencing them or removing unused labels to keep the source clean.

8. **[LOW] Keywords/classification codes** -- Add if venue requires them.
